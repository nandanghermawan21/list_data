name: list_data
description: A new Flutter package project.
version: 1.0.1
publish_to: none
homepage: "list_data"

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter:
    git:
      url: https://github.com/Renoldi/faf
  skeleton_text: ^3.0.0
  flutter_widget_from_html: ^0.16.0
  error_handling:
    git:
      url: https://github.com/Renoldi/error_handling
      ref: 1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
